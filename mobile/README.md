# Sanjhi — Mobile (Android & iOS)

React Native app. Mirrors the web app's feature structure for consistency
(src/features/*), with mobile-specific navigation in src/navigation/.

Native project folders (android/, ios/) are generated by the React Native CLI
and kept here as placeholders.
