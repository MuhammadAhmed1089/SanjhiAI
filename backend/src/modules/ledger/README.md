# ledger module

Maps to: FR-LEDGER-01 — role-scoped ledger views (full ledger for Organizer/Co-Organizer, private + aggregate view for Members).

Suggested contents: controllers/routes, service logic, and module-local validation
for this domain. Keep DB models in backend/src/db/models.
